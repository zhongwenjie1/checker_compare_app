# .venv312/lib/python3.12/site-packages/pandas/tests/indexes/base_class/test_where.py

## Classes & Methods
{
  "classes": [
    {
      "name": "TestWhere",
      "methods": [
        "test_where_intlike_str_doesnt_cast_ints"
      ]
    }
  ],
  "functions": []
}

## Head Snippet
```
import numpy as np

from pandas import Index
import pandas._testing as tm


class TestWhere:
    def test_where_intlike_str_doesnt_cast_ints(self):
        idx = Index(range(3))
        mask = np.array([True, False, True])
        res = idx.where(mask, "2")
        expected = Index([0, "2", 2])
        tm.assert_index_equal(res, expected)

```
