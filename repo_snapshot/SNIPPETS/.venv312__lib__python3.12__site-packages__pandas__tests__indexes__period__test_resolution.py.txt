# .venv312/lib/python3.12/site-packages/pandas/tests/indexes/period/test_resolution.py

## Classes & Methods
{
  "classes": [
    {
      "name": "TestResolution",
      "methods": [
        "test_resolution"
      ]
    }
  ],
  "functions": []
}

## Head Snippet
```
import pytest

import pandas as pd


class TestResolution:
    @pytest.mark.parametrize(
        "freq,expected",
        [
            ("Y", "year"),
            ("Q", "quarter"),
            ("M", "month"),
            ("D", "day"),
            ("h", "hour"),
            ("min", "minute"),
            ("s", "second"),
            ("ms", "millisecond"),
            ("us", "microsecond"),
        ],
    )
    def test_resolution(self, freq, expected):
        idx = pd.period_range(start="2013-04-01", periods=30, freq=freq)
        assert idx.resolution == expected

```
